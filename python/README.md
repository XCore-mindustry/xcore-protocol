# Python Package

This area contains generated Python protocol models plus thin handwritten validation and test support around the generated surface.

Application repositories should depend on these generated artifacts instead of maintaining separate hand-written wire models.
