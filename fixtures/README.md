# Fixtures

Fixtures are the golden examples for protocol validation.

- `valid/` — canonical examples that must pass
- `invalid/` — examples that must fail strict validation
- `legacy/` — historical examples accepted only by explicit compatibility logic
