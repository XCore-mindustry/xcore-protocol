# Scripts

This area contains repository helper scripts for generation and validation workflows.

Current entrypoints include:
- `validate-generation.py` for regeneration/stale-output checks
- `validate-generated-models.py` for generated-model and cross-language compatibility validation
